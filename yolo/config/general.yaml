device: 0
cpu_num: 16 # Number of worker threads per GPU

image_size: [640, 640]

out_path: runs
exist_ok: True

lucky_number: 10 # Deterministic Random Number Seed
use_wandb: True
use_tensorboard: False

weight: True # Path to weight or True for auto, False for no pretrained weight

# Global step logging interval for Trainer
log_every_n_steps: 500
